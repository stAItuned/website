# Backend config for App Hosting PROD backend (branch: master)

runConfig:
  cpu: 1
  memoryMiB: 1024
  minInstances: 0
  maxInstances: 10
  concurrency: 100

env:
  - variable: APP_ENV
    value: prod
    availability:
      - BUILD
      - RUNTIME

