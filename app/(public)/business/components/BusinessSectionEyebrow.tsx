export function BusinessSectionEyebrow({ children }: { children: string }) {
  return (
    <p className="text-sm font-semibold uppercase tracking-[0.22em] text-primary-500 dark:text-amber-300">
      {children}
    </p>
  )
}
