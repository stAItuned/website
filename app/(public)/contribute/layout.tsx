import './brand-tokens.css'
/**
 * Layout for the contribute area, including the feedback widget.
 */
export default function ContributeLayout({
    children,
}: {
    children: React.ReactNode
}) {
    return (
        <>{children}</>
    )
}
