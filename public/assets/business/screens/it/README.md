# Business Workflow Screens (IT)

These files are wired in `/business` workflow carousel:

- `operator-1.png`
- `operator-3.png`
- `manager-3.png`
- `admin-1.png`
- `admin-2.png`

These PNG files match the wireframe proportions used by the carousel and replace the old SVG placeholders.

Requirements for replacement exports:

- format: `png` or `webp`
- short side: at least `536px`
- vertical ratio close to `9:19.5`
- if switching assets again, update the paths in `app/(public)/business/page.tsx`
